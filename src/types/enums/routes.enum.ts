export enum AppRoutes {
    MAP = "/",
    CHARACTERS = "/characters",
    CHARACTER = "/characters/:characterName",
    TOOLS = "/tools",
    TOOL = "/tools/:toolName",
    HISTORY_LOG = "/history",
    PROFILE = "/profile/:id"
}