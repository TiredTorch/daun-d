export enum AppRoutes {
    MAP = "/world-map",
    CHARACTERS = "/characters",
    CHARACTER = "/characters/:characterName",
    TOOLS = "/tools",
    TOOL = "/tools/:toolName",
    HISTORY_LOG = "/history",
    PROFILE = "/profile"
}